<Project Sdk="Godot.NET.Sdk/4.3.0">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <LangVersion>latest</LangVersion> <!-- Optional, ensures latest C# features -->
    <Nullable>enable</Nullable>
    <TargetFramework Condition=" '$(GodotTargetPlatform)' == 'android' ">net8.0</TargetFramework>
    <TargetFramework Condition=" '$(GodotTargetPlatform)' == 'ios' ">net8.0</TargetFramework>
    <EnableDynamicLoading>true</EnableDynamicLoading>
    <RootNamespace>ld56warmup</RootNamespace>
  </PropertyGroup>
<ItemGroup>
  <Content Remove="script_templates\**" />
  <Compile Remove="script_templates\**" />
  <EmbeddedResource Remove="script_templates\**" />
  <None Remove="script_templates\**" />
</ItemGroup>

</Project>
